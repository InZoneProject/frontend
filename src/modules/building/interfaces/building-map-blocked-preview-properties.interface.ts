import type { BlockedGeometryPreview } from '@/modules/building/interfaces/blocked-geometry-preview.interface'

export interface BuildingMapBlockedPreviewProperties {
    preview: BlockedGeometryPreview
}
